%html
  %head
    %title Problems
  %body
    %h1 Problems
    - @problems.each do |prob|
      %img{:src => prob.pic.url}
      %h2= "#{prob.title}"
      %h3= "#{prob.desc}"
      %h3= "#{prob.lat}, #{prob.long}"
      %h3= "#{prob.user}"
      %br
