.block
  .block-head
    %h2 Trails
  .block-content
    - @photos.keys.each do |trail|
      %h3
        %a{:href => "/trails/#{trail}"}
          = "#{trail.capitalize}"
      /- @photos[trail].each do |image|
      /  %img{:src => "#{image.pic.url}"}